/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "skillicons.dev",
      "www.stefantopalovic.com",
      "www.passwarden.com",
    ],
  },
};

export default nextConfig;
